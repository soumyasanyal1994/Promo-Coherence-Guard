# Promo-Coherence-Guard
Promo Coherence Guard ingests a batch of active promotion rules and product pricing data, applies a deterministic conflict-detection layer, and uses an LLM to generate a plain-English conflict report with severity ratings and remediation recommendations. The goal is to catch pricing errors before they go live, not after a loss-making trading day.
